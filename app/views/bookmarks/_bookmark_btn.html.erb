<% if post.bookmarked_by(current_user) %>
  <%= link_to "保存済み", post_bookmarks_path(post), method: :delete, remote: true, class: "fas fa-bookmark text-dark" %>
<% else %>
  <%= link_to "保存する", post_bookmarks_path(post), method: :post, remote: true, class: "far fa-bookmark text-body" %>
<% end %>