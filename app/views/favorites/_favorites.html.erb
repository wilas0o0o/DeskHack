<% if favorited_by?(current_user) %>
  <%= link_to post.favorite.count, post_favorites_path(post), class: "fas fa-heart heart-red" %>
<% else %>
  <%= link_to post.favorite.count, post_favorites_path(post), method: :delete, class: "fas fa-heart heart-blue" %>
<% end %>