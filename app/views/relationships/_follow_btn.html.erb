<% if current_user.followings?(user) %>
  <%= link_to "フォロー中", user_relationships_path(user), method: :delete, remote: true, class: "btn btn-secondary" %>
<% else %>
  <%= link_to "フォローする", user_relationships_path(user), method: :post, remote: true, class: "btn btn-primary" %>
<% end %>